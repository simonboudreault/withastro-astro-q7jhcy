---
export const prerender = false;
import ConversationList from '../vue/ConversationList.vue';
const navItems = [{ name: 'Home', href: '/' }];

// You would typically get this from your auth service
const isLoggedIn = false;
const message = 'Astro is awesome!';
---

<nav class="bg-white shadow-lg">
  {
    navItems.map((item) => (
      <a href class="block px-4 py-2 text-gray-800 hover:bg-gray-100" href={item.href}>
        {item.name}
      </a>
    ))
  }

  <div>sidebar</div>
  <ConversationList client:only="vue" />
</nav>

<script define:vars={{}}></script>
